<div class="jazzquiz-box {{# instructor }}instructor{{/ instructor }}">
    <form id="jazzquiz_question_form" action="" method="post" enctype="multipart/form-data" accept-charset="utf8" onsubmit="return false;">
        <div class="jazzquiz-response-container">{{{ question }}}</div>
        <input type="hidden" name="slot" value="{{ slot }}">
        {{^ instructor }}
        <div class="save_row">
            <div class="jazzquiz-save-question">
                <button class="btn btn-primary">Save</button>
            </div>
        </div>
        {{/ instructor }}
    </form>
</div>